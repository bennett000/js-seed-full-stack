/*global describe, it, expect, spyOn, beforeEach, module, inject, afterEach */

describe('users service', function () {
    'use strict';

    beforeEach(function () {
        module('MealCalories');
    });
});

