<!doctype html>
<html lang="en" ng-app="MealCalories">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
    <title>Calorie Counter</title>
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes"/>
    <meta name="apple-mobile-web-app-status-bar-style" content="black">
    <meta name="viewport"
          content="initial-scale=1.0, height=device-width, width=device-height, maximum-scale=1.0, user-scalable=0"/>
    <% styles.forEach(function (style) { %>
    <link href="<%= style %>" rel="stylesheet"><% }); %>
    <style>
        body, html {
            background-color: black;
            margin: 0;
        }
    </style>
</head>
<body>
<noscript>
    Sorry, but this calorie tracker requires JavaScript to function.
</noscript>

<meal-calories></meal-calories>

<% scripts.forEach(function (script) { %>
<script src="<%= script %>"></script><% }); %>
</body>
</html>
